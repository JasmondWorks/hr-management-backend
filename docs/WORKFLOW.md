# Start infra (once, or after a reboot)

```bash
npm run infra:up
```

# Start the server (watch mode, hot-reloads on save)

```bash
npm run dev
```

# When you change the schema

```bash
npm run db:migrate -- --name your_change_description
```
